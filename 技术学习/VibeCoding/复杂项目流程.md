# PythonUV PythonTest?
- PythonTest?ts是否有类似的


## 确定需求
- 目标：给出指定目标
- 输入：描述当前项目结构现有数据
- 输出：proposal.md需求文档
- 步骤：描述你知道的需求相关信息，与你不清楚的，让ai进行提问
    ### 关键描述
    - 请使用提问的方式帮助我确认需求。
## 设计
- 概要设计 划分模块
- 详细设计 实现细节
- 目标：根据需求文档生成详细设计文档
- 输入：需求文档 (项目规范文档)
- 输出：详细设计文档detailed-design.md
- 步骤：
    ### 关键描述
    - 根据需求文档的内容，根据里面划分出的模块编写详细设计文档。
    - 模块与模块之间尽量保持相互独立，可以独立进行测试。
    - ？尽量符合规范文档进行编写
    - 不要猜测我的意图。任何不明确的地方都必须都向我提问。
## 划分任务
- 避免上下文过长将需求错误理解
- 目标：为每个模块划分最小可执行任务
- 输入：需求文档和详细计划文档
- 输出：每个模块文档，总体进度文档
- 步骤：根据需求文档和详细设计。
    ### 关键描述
    - 为每一个模块生成Vibe Coding用的最小任务。
    - 每个模块对应一个 <module-name>.md
    - progress.md中 用check list表示模块是否已经完成
## 实现
- 通过创建一个 progress.md 创建监工来生成子agent完成需求
- 目标：生成Vibe Coding用的Prompt
- 输入：需求文档、详细设计、任务划分
- 输出：prompt.md
- 步骤：阅读输入信息，了解当前要实现的工程
    ### 关键描述
    - 生成doc/prompt.md作为Vibe Coding的起始Prompt
    - 主Agent，用来跟踪整体的进度
    - 主Agent生成子Agent，用来实现每一个模块，并完成测试
    - 整个过程不会有人工参与
    - 代码必须有完整的pytest单元测试
    - 并通过mypy和ruff检测
    - 生成prompt过程中
    - 如果有任何不明确的地方都必须都向我提问。

# 细节补充
- 上下文过长被压缩 即时保存到md文档 保持上文简短 ？？/btw |claude code
- pytest 库
- ow架构
- 权限管理 policy
- docker
- 提示词注入
- ts测试？