整理 [子系统名称] 的代码结构，输出一份架构文档。
     
## 要求
    
### 内容结构
1. **总览** — 分层/分模块的架构图（Mermaid），各模块职责一览表  2. **核心数据结构** — 关键 class/struct 的类图（Mermaid），标注字段用途和生命周期
3. **初始化流程** — 从启动入口到就绪状态的完整时序图，标注每个步骤的触发时机  4. **核心业务流程** — 选取 1-2 个典型调用场景，画出调用链序列图，附源码片段
5. **模块间交互** — 用流程图展示模块间的数据流和依赖关系
6. **附录** — 源码路径索引表 / 常用 API 速查表 / 设计约定或注意事项
7. **完整源代码** — 每个核心类的完整实现代码，去除注释后保留全部逻辑

### 项目实现代码要求
- **必须从项目中读取实际源文件**，不可凭空编造代码
- 每个核心方法附上精简后的真实源码片段（去掉注释和日志，保留核心逻辑）
- 代码片段需标注所在文件路径和行号范围，便于读者直接跳转
- 覆盖关键分支：区分 Editor 模式 / 真机模式 / WebGL 等平台差异
- 给出至少一个完整的业务调用示例，从上层 Controller 到底层框架

### 完整源代码要求
- 在文档末尾或独立章节中，**逐文件贴入核心类的完整实现代码**
- 使用 ```csharp 代码块，文件路径作为标题
- 去除原有注释和日志输出语句，保留全部业务逻辑和方法实现
- 每个文件用分隔线隔开，标题格式：`### 文件: Assets/Libs/KitCore/.../ClassName.cs`
- 涵盖范围：子系统的所有 partial class 文件、关键接口定义、核心数据结构

### 输出格式
- Markdown 文件，图表用 Mermaid 语法（GitHub/GitLab 直接渲染）
- 每个章节标注对应的源文件路径
- 代码段用 ```csharp 格式，只保留核心逻辑，去掉冗余注释

### 分析要求
- 从入口文件开始，追踪调用链，理清每层的职责和接口
- 识别 partial class 的分布，找出同一个类的所有文件
- 区分 Editor 模式 / 真机模式的分支逻辑
- 注明引用计数、缓存、对象池等内存管理策略
- **基于实际代码推导设计意图**：代码中出现的约束（如排队机制、Prefab缓存）要解释其存在的原因

### 交付
- 路径：[F:\Goethe\Notes\Work-Summary\1-代号灵业务\业务\项目底层工具]/[子系统名称]-架构.md
- 只描述现状，不做重构建议