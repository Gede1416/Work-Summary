# 功能提示小助手 — 类型 6 / 类型 7 待补全清单

> 当前状态：核心逻辑已实现，美术资源未就绪，以下位置暂时用 `Debug.Log` 占位。

---

## 1. 建筑图标加载

**文件**: `Assets\Scripts\Hot\Logic\UI\Main\MainUIController_Assistant.cs`

### 1.1 类型 7 图标 — 第 147 行

```
imgIconAssistant2.gameObject.SetActive(false);
Debug.Log($"[10652] 显示类型7: heroUuid={forgeHeroUuid}, buildingType={forgeBuildingType}");
```

**需补全为**：根据 `forgeBuildingType` 加载对应建筑小图标

```csharp
// 参考类型2-5的图标加载模式：
// var iconPath = GetBuildingIconPath(forgeBuildingType); // 路径需确认
// imgIconAssistant2.sprite = ImageUtil.GetInstance().GetSpriteByPath(iconPath);
// imgIconAssistant2.gameObject.SetActive(true);
```

### 1.2 类型 6 图标 — 第 154 行

```
imgIconAssistant2.gameObject.SetActive(false);
Debug.Log($"[10652] 显示类型6: heroUuid={forgeHeroUuid}, buildingType={forgeBuildingType}");
```

**需补全为**：同上，加载建筑小图标

---

## 2. 类型 6/7 独立按钮

**文件**: `Assets\Scripts\Hot\Logic\UI\Main\MainUIController_Assistant.cs`

### 2.1 按钮绑定 — 第 27 行

当前临时共用 `btnAssistant2`：

```
this.btnAssistant2.OnClick += _ => OnAssistantProductionRowClick();
```

**需改为**：类型 6/7 使用独立按钮（如新增 `btnAssistant3`），类型 2-5 保留 `btnAssistant2`

### 2.2 按钮显隐 — 第 186 行

当前 `btnAssistant2` 承载所有类型 2-7：

```
btnAssistant2.gameObject.SetActive(showTopRow);
```

**需改为**：类型 6/7 独立控制显隐，如 `btnAssistant3.gameObject.SetActive(showForgeHint);`

### 2.3 prefab 新增按钮

MainUI.prefab 需新增 `btnAssistant3` + `UITextPro txAssistant3` + `Image imgIconAssistant3`，
并在 `MainUIController.Designer.cs` 中生成对应的序列化字段。

---

## 3. 引导手指生成

**文件**: `Assets\Scripts\Hot\Logic\UI\Main\MainUIController.cs`

### 3.1 手指 prefab 生成 — 第 654～664 行

```
// TODO: 美术资源就绪后，取消注释以下代码启用引导手指
// var heroCtrl = MapObjectsMgr.GetHeroModel((ulong)forgeHeroUuid);
// if (heroCtrl != null)
// {
//     var fingerPrefabPath = "prefab/ui/guide/guide_finger.prefab"; // 路径待定
//     GameObjectPool.UI3D.SpawnAsync(fingerPrefabPath, 0, item =>
//     {
//         guideFingerItem = item;
//         item.gameObject.transform.position = heroCtrl.transform.position + Vector3.up * 3f;
//     });
// }
```

**需确认**：
- 引导手指 prefab 路径（是否复用 GuideSystem 现有资源，还是新建）
- 手指在世界空间的位置偏移（当前写死 `Vector3.up * 3f`）

---

## 4. 引导手指全局点击清除

**文件**: `Assets\Scripts\Hot\Logic\UI\Main\MainUIController.cs`

### 4.1 点击监听 — 第 666 行

```
// TODO: 美术资源就绪后，启用全局点击监听清除手指
```

**需实现**：玩家点击屏幕任意位置 → 调用 `DismissGuideFinger()`（方法框架已就绪）

实现方案参考：
- 方案 A：添加一个全屏透明 `UIButton` 覆盖在最上层，OnClick 触发清除
- 方案 B：在 `Update()` 中监听 `Input.GetMouseButtonDown(0)`，检测到点击后清除
- 方案 C：注册全局 GuideSystem 点击事件

---

## 5. 美术资源汇总

| 资源 | 用途 | 当前状态 | 备注 |
|------|------|----------|------|
| 建筑小图标（103/107/108） | 类型 6/7 按钮图标 | Debug.Log 占位 | 美术未上传 |
| 举牌小人 prefab | 类型 6/7 UI 形象 | 未实现 | 建议新建 |
| 引导手指 prefab | 类型 7 手指指引 | 路径待定 | 可能复用 GuideSystem 资源 |

---

## 6. 配置表确认项

| 项目 | 当前值 | 待确认 |
|------|--------|--------|
| text_info ID（类型6） | 900525 | 需策划确认 |
| text_info ID（类型7） | 900526 | 需策划确认 |
| assistant 表 Type=6 | `##0##正在前往##1##` | 已配置 |
| assistant 表 Type=7 | `##0##已到达##1##` | 已配置 |
